1、可重载的操作符（42个）
  + - * / % ^ 
  & | ~
  …
  
 2、不能重载的操作符（4个）
  :: .*
  .  ?:

  
 3、重载操作符的注意事项
  ①不允许修改重载操作符的数据类型
 
  ①赋值 operator=
  ②下标 operator[]
  ③函数调用 operator()
  ④成员访问 operator->
  这4个要么不重载，要么定义为成员函数，不能作为非成员函数
  
  
  
